# Legacy-Templates

`index_legacy.html` — Alter monolithischer SPA-Stand (v0.10.49).
Nicht mehr als Default aktiv. Nur als Referenz/Fallback.
Kann entfernt werden wenn keine Regressions-Tests mehr dagegen laufen.
