# PiDrive: PulseAudio-Compat-Socket für System-Mode
# Alle bestehenden PULSE_SERVER=unix:/var/run/pulse/native bleiben unverändert
context.properties = {
    server.address = [
        "unix:/var/run/pulse/native"
    ]
}
